import React from 'react';

const DownloadFiles = (audio_objs, profile_objs) =>  {

    const handleDownload = (e) => {

    }

    return(
      <div>
            <button onClick={handleDownload}>Download All</button>
      </div>
    );
}